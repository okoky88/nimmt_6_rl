# Installation
`pip install numpy`

`pip install torch`

`mkdir model`

## Training
`python train.py`

This will generate a file model/6nimmt_dqn_final.pth

## Test
`python test_trained_model.py`

After training with 200 episodes

```
After 10000 trials:
RL Agent wins: 10000
Random Bots wins: 0
```
